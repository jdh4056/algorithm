#include <bits/stdc++.h>
using namespace std;

int main() {
    while(true) {
        int w, h;
        cin >> w >> h;

        if (w == 0 && h == 0) {
            break;
        }

        int island = 0;
        int
        for (int i = 0; i < h; ++i) {

        }
    }
}